const add = function(x, y) {
    console.log(y)
    let sum = (x + y);
    return sum
};

// let value = add(1, 2)
// console.log(value)

const multiply = function(x, y) {
    let multiply = (x * y);
    return multiply
};

// let value = multiply(8, 2)
// console.log(value)

const divide = function(x, y) {
    let divide = (x / y);
    return divide
};

// let value = divide(8, 2)
// console.log(value)

const subtract = function(x, y) {
    let subtract = (x - y);
    return subtract
};

let value = subtract(8, 2)
console.log(value)